apiVersion: v1
kind: Service
metadata:
  name: {{ include "evote.fullname" . }}
  labels:
    {{- include "evote.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "evote.selectorLabels" . | nindent 4 }}
